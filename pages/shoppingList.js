import React from 'react';
import { ShoppingList } from '../src/components/connections';
import { Head, Navigation } from '../src/components';

export default function ShoppingListPage() {
    return (
      <x>
        <Head />
        <Navigation />
        <ShoppingList />
      </x>
    );
}
